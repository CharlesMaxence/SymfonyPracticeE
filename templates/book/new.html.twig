{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
<div class="container">
    <h1>{{"Create new Book"|trans}}</h1>

    {{ include('book/_form.html.twig') }}

    <a href="{{ path('app_book_index') }}"><button class="btn btn-primary">{{"back to list"|trans}}</button></a>
</div>
{% endblock %}
