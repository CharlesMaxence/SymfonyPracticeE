{% extends 'base.html.twig' %}

{% block title %}New Author{% endblock %}

{% block body %}
<div class="container">
    <h1>{{"Create new Author"|trans}}</h1>

    {{ include('author/_form.html.twig') }}

    <a href="{{ path('app_author_index') }}"><button class="btn btn-primary">{{"back to list"|trans}}</button></a>
</div>
{% endblock %}
