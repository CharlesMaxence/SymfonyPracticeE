{% extends 'base.html.twig' %}

{% block title %}{{"New Theme"|trans}}{% endblock %}

{% block body %}
    <h1>{{"Create new Theme"|trans}}</h1>

    {{ include('theme/_form.html.twig') }}

    <a href="{{ path('app_theme_index') }}">{{"back to list"|trans}}</a>
{% endblock %}
